server:
  error:
    include-message: always

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:greentrails;DB_CLOSE_DELAY=-1
    username: mock
    driver-class-name: org.h2.Driver